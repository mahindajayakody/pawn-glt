DROP TABLE tblClosureType;

CREATE TABLE tblClosureType(
	CLOSURETYPEID INT PRIMARY KEY AUTO_INCREMENT,
	CODE CHAR(3),
	DESCRIPTION VARCHAR(50),
	RECSTATUS SMALLINT,
	LASTUPDATE DATETIME,
	LASTUPTIME LONG,
	LASTUPUSERID INT,
	ORIGINALID INT,
	VERSIONID INT,
	COMID INT,
	BRANCHID INT
);
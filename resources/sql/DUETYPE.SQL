DROP TABLE tblDueType;

CREATE TABLE tblDueType(
	DUETYPEID INT PRIMARY KEY AUTO_INCREMENT,
	PRDID CHAR(2),
	CODE CHAR(3),
	DESCRIPTION VARCHAR(50),
	SEQUENCE SMALLINT,
	ISINTERNAL VARCHAR(2),
	ISRECEIPT VARCHAR(2),
	ISODCHARGABLE VARCHAR(2),
	RECSTATUS SMALLINT,
	LASTUPDATE DATETIME,
	LASTUPTIME LONG,
	LASTUPUSERID INT,
	ORIGINALID INT,
	VERSIONID INT,
	BRANCHID INT,
	COMID INT

);

ALTER TABLE `pawn`.`tblduetype` ADD COLUMN `ACCOUNTNO` VARCHAR(20) AFTER `COMID`;